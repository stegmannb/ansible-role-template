[project]
name = "ansible-role-template"
version = "0.1.0"
description = "Ansible Template Role"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=11.2.0",
    "ansible-lint>=25.1.2",
    "molecule>=25.2.0",
    "pre-commit>=4.1.0",
]
